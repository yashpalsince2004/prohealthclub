---
import Layout from "../layouts/Layout.astro";
import ForgotPasswordForm from "../components/ForgotPasswordForm";
import heroBg from "../assets/hero-bg.jpg";
import logo from "../assets/logo.jpg";
---

<Layout title="Forgot Password - Prro Health Club" description="Recover your account password at Prro Health Club.">
  <div class="relative h-screen w-screen max-h-screen overflow-hidden flex items-center justify-center bg-[#070c0e] p-6 lg:p-8 z-10">
    
    <!-- Premium background glowing gradients to simulate dark marble atmosphere -->
    <div class="absolute inset-0 pointer-events-none z-0">
      <div class="absolute -top-40 -left-40 w-[600px] h-[600px] rounded-full bg-primary/10 blur-[150px]"></div>
      <div class="absolute -bottom-40 -right-40 w-[600px] h-[600px] rounded-full bg-[#0e4859]/20 blur-[150px]"></div>
      {/* Subtle grid pattern overlay */}
      <div class="absolute inset-0 bg-[linear-gradient(to_right,#8080800a_1px,transparent_1px),linear-gradient(to_bottom,#8080800a_1px,transparent_1px)] bg-[size:24px_24px]"></div>
    </div>

    <a 
      href="/login" 
      class="absolute top-10 left-10 text-sm font-semibold text-gray-400 hover:text-white transition-colors flex items-center gap-2 group z-50 bg-black/30 backdrop-blur-md px-4 py-2 rounded-full border border-white/10"
    >
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="group-hover:-translate-x-1 transition-transform">
        <path d="m15 18-6-6 6-6"/>
      </svg>
      Back to Login
    </a>

    <!-- Outer Card -->
    <div class="w-full h-full bg-[#111820] rounded-[2.5rem] overflow-hidden grid lg:grid-cols-12 shadow-[0_0_80px_-15px_rgba(255,92,36,0.25)] border border-primary/30 relative z-10 p-4 gap-4">
      
      {/* Left Column - Image & Branding Card (Hidden on Mobile) */}
      <div class="lg:col-span-5 relative hidden lg:flex flex-col justify-between p-10 rounded-[1.8rem] overflow-hidden h-full">
        {/* Background Image */}
        <img 
          src={heroBg.src} 
          alt="Gym Interior" 
          class="absolute inset-0 w-full h-full object-cover scale-105"
        />
        {/* Dark overlays to ensure text readability */}
        <div class="absolute inset-0 bg-black/40 z-10"></div>
        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent z-10"></div>

        {/* Content Overlay */}
        <div class="relative z-20 flex flex-col justify-between h-full">
          {/* Logo & Header */}
          <div class="flex items-center space-x-3">
            <img 
              src={logo.src} 
              alt="Prro Health Club Logo" 
              class="h-10 w-10 object-contain rounded-full border border-white/20"
            />
            <span class="text-lg font-black text-white tracking-wider">Prro Health Club</span>
          </div>

          {/* Motivational Text */}
          <div class="space-y-4 text-left">
            <h3 class="text-3xl font-black leading-tight uppercase tracking-tight text-white">
              Transform <br />
              Your <span class="text-primary text-glow">Limits</span> !
            </h3>
            <p class="text-xs text-gray-300 leading-relaxed font-semibold">
              Recover access to your account and continue your consistent discipline. We are here to support you at every milestone.
            </p>
            <div class="text-[10px] font-bold text-gray-400 uppercase tracking-widest pt-2">
              Strength in consistency.
            </div>
          </div>
        </div>
      </div>

      {/* Right Column - Forgot Password Form Container */}
      <div class="lg:col-span-7 flex items-center justify-center p-6 sm:p-10 bg-transparent h-full overflow-y-auto">
        <ForgotPasswordForm client:load />
      </div>

    </div>
  </div>
</Layout>
